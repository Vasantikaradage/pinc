package com.pinc.android.MB360.insurance.enrollment.interfaces;

public interface ParentalDetailsHelper {
    //here we made the helping interfaces for the recyclerview for
    // adding the parent
    void onParentAdd(int position);
    void onParentDelete(int position);
}
